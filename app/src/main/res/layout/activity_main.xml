<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:background="@drawable/login_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">
    <ScrollView android:layout_width="match_parent"
                android:layout_centerVertical="true"
                android:layout_height="wrap_content">

        <LinearLayout android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="vertical"
                      android:layout_marginStart="16dp"
                      android:layout_marginEnd="16dp">

            <ImageView android:layout_width="wrap_content"
                       android:layout_height="wrap_content"
                       android:src="@drawable/bookworm"
                       android:layout_gravity="center_horizontal"/>

            <EditText android:layout_width="match_parent"
                      android:layout_marginTop="20dp"
                      android:drawableStart="@drawable/ic_user"
                      android:hint="Email"
                      android:drawablePadding="10dp"
                      android:layout_height="wrap_content"
                      android:background="@drawable/login_edit_background"
                      android:padding="10dp"
                      android:id="@+id/emailTxt" android:textColor="#FFFFFF" android:textColorHint="#FAF7F7"/>

            <EditText android:layout_width="match_parent"
                      android:layout_marginTop="10dp"
                      android:drawableStart="@drawable/ic_password"
                      android:hint="Password"
                      android:drawablePadding="10dp"
                      android:layout_height="wrap_content"
                      android:background="@drawable/login_edit_background"
                      android:padding="10dp" android:id="@+id/loginPwd" android:password="true"
                      android:textColor="#F7F4F4" android:textColorHint="#F8F7F3F3"/>

            <TextView android:layout_width="wrap_content"
                      android:text="Forgot Password?"
                      android:id="@+id/forgot"
                      android:padding="10dp"
                      android:layout_marginTop="10dp"
                      android:layout_height="wrap_content"
                      android:layout_gravity="end" android:textColor="#FA9B08"/>

            <Button android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Log In"
                    android:layout_marginTop="30dp"
                    android:layout_marginBottom="30dp"
                    android:textAllCaps="false"
                    android:textSize="21sp"
                    android:background="@drawable/button_drawable" android:id="@+id/loginBtn"/>

            <TextView android:layout_width="wrap_content"
                      android:text="Sign Up"
                      android:id="@+id/btnReg"
                      android:textSize="21sp"
                      android:layout_height="wrap_content"
                      android:layout_gravity="center_horizontal" android:textColor="#FA9B08"/>

        </LinearLayout>

    </ScrollView>
</RelativeLayout>